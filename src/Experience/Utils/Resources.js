import EventEmitter from "./EventEmitter.js";

export default class Resources extends EventEmitter {

  constructor() {
    super() 

  }
}

// Will probably need this for stuff other than the json anyway, so keep for now.